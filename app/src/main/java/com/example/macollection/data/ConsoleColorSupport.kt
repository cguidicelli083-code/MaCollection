package com.example.macollection.data

/**
 * Aide affichée sous le bouton 3D. Le changement de couleur est disponible pour tous
 * les modèles, mais le rendu dépend de la façon dont chaque modèle est construit
 * (couleur unie vs texture) — ce n'est pas prévisible de façon fiable.
 */
object ConsoleColorSupport {
    const val HINT = "🎨 Couleur modifiable dans la vue 3D (le rendu dépend du modèle)"
}
