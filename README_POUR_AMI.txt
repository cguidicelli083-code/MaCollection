========================================================
  MA COLLECTION  -  CODE SOURCE (a reprendre / modifier)
========================================================
Android natif : Kotlin + Jetpack Compose + Room (KSP)
compileSdk 34 | minSdk 24 | JDK 17 | ABI arm64-v8a

REPRENDRE AVEC CLAUDE CODE :
1. Dezippe ce dossier.  2. Ouvre Claude Code dedans.
3. Dis : "Reprends ce projet Android MaCollection, lis le code et le LISEZMOI."

COMPILER (Android Studio) :
1. Open -> ce dossier.
2. local.properties est DEJA fourni (cles API incluses). Verifie juste sdk.dir
   (Android Studio l'ajoute automatiquement a l'ouverture).
3. Gradle se synchronise -> branche un telephone (USB) -> Run.

Voir LISEZMOI.txt pour "ce qui marche / reste a faire".
========================================================
